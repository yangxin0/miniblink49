tmp/*
*.opensdf
*.sdf
build/.vs/*
build/miniblink.VC.db
build/miniblink.VC.VC.opendb
*.ipch
out/*
bin/cookies.dat
*.user

# CMake (cross-platform build) out-of-source build dirs
build-cmake/
cmake-build-*/
build-win*/
/build/CMakeFiles/
CMakeCache.txt

# Legacy Visual Studio project/solution files — removed in favour of the
# cross-platform CMake build. Ignored so they cannot be re-introduced.
*.sln
*.vcxproj
*.vcxproj.filters
*.vcxproj.user
*.vcproj
*.suo

# Compiled build artifacts (object files + static archives)
*.o
*.a

# macOS CMake out-of-source build directories (all generated): build-mac (release),
# build-mac-debug, and any other build-mac* variant.
build-mac*/
