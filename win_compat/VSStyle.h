// macOS shim for <VSStyle.h> (Windows visual-styles part/state IDs). content/ui/
// CustomTheme.h includes it but references no symbols on the paths we build. Empty
// shim so the include resolves.
#pragma once
